@page
@model Pharmacy.Pages.Prescriptions.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Prescription</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Prescription.PrescriptionId" />
            <div class="form-group">
                <label asp-for="Prescription.Patient.FullName" class="control-label"></label>
                <select asp-for="Prescription.Patient.PatientId" class="form-control" asp-items="Model.Patients"></select>
                <span asp-validation-for="Prescription.Patient.FullName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Prescription.TotalPrice" class="control-label"></label>
                <input asp-for="Prescription.TotalPrice" class="form-control" />
                <span asp-validation-for="Prescription.TotalPrice" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
